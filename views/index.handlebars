<div class="p-5 mb-4 bg-light rounded-3">
  <div class="container-fluid py-5">
    <h1 class="display-5 fw-bold">Bem vindo ao Blog do Node</h1>
    <p class="col-md-8 fs-6">Esse é um BLOG simples, desenvolvido em Node.js + Express + MongoDB</p>
    <hr>
    <small>Aqui você vai ler sobre todas as novidades do mundo da computação.</small>
    <br>
    <a href="/usuarios/registro"><button class="btn btn-primary mt-4" type="button">Criar uma conta</button></a>
  </div>
</div>
<hr>
<h2>Postagens recentes</h2>
{{#each postagem}}
  <div class="card mb-2">
    <div class="card-body">
      <h3>{{titulo}}</h3>
      <p>Descrição: {{descricao}}</p>
      <a href="/postagem/{{slug}}"><button class="btn btn-dark">Ler mais</button></a>
      <hr>
      <small>Categoria: {{categoria.nome}}</small>
      <br>
      <small>{{data}}</small>
    </div>
  </div>
{{else}}
  <div class="container">
    <h3 class="container">Nenhuma postagem encontrada!</h3>
  </div>
{{/each}}